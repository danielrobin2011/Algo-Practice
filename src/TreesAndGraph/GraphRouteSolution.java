package TreesAndGraph;

import java.util.LinkedList;

public class GraphRouteSolution {

    boolean search(Graph graph, Node start, Node end) {
        if (start == end) {
            return true;
        }
        LinkedList<Node> q = new LinkedList<>();

        q.add(start);

        Node u;
        while (!q.isEmpty()) {
//            u = q.remo
        }


        return false;
    }

}
